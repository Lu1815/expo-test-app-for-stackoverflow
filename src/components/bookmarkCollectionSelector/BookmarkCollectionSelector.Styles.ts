
import { StyleSheet } from 'react-native';

export const bookmarkcollectionselectorStyles = StyleSheet.create({
    text: {
        fontSize: 16,
        fontWeight: 'bold',
        textAlign: 'center',
    }
})
